public class Mat {
    public static void main(String[] args) {
        // Utilizzato per trovare il valore più basso
        Math.max(5, 10);
        // Utilizzato per trovare il valore più basso
        Math.min(5, 10);
        // Restituisce la radice quadrata
        Math.sqrt(64);
        // Restituisce il valore assoluto di x
        Math.abs(-4.7);
        // Restituisce un numero causale tra tra 0.0(incluso) e 1.0(escluso)
        Math.random();
        // Si può ottenere più controllo sul numero casuale con la seguente formula
        int randomNum = (int)(Math.random() * 101);
    }
}
